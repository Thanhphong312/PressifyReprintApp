# create table product_reprint
    - name
# create table color_reprint
    - name
# create table size_reprint
    - name

# table reprints thêm product_reprint_id, color_reprint_id, size_reprint_id 
    - thêm field
    - chuyển code sang dùng field mới
